#ifndef OPTIES_H
#define OPTIES_H

// Temperature threshold (in degrees Celsius) to turn the fan ON
#define FAN_TEMP_THRESHOLD 30.0f

// Time (in milliseconds) the fan remains ON after temperature drops below threshold
#define FAN_OFF_DELAY_MS 1800000  // 30 minutes

#endif // OPTIES_H
